## 1. Prompt and architecture updates

- [x] 1.1 Update `prompts/dispatcher.md` so the Dispatcher explicitly cannot implement code or perform product-logic edits.
- [x] 1.2 Update `AGENT_ARCHITECTURE.md` to state that the Dispatcher is coordination-only, not an implementation agent.
- [x] 1.3 Verify the single-agent path wording still allows direct handling of simple non-code coordination work.

## 2. Validation

- [x] 2.1 Confirm the change spec reflects the implementation boundary accurately.
- [x] 2.2 Review the final wording for clarity so future contributors do not reintroduce the ambiguity.
